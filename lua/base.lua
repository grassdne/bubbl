title = "CHANGE ME"

OnUpdate = function(dt) end

OnMouseMove = function(x, y) end

OnMouseUp = function(x, y) end

OnMouseDown = function(x, y) end

OnKey = function(key, down) end

OnWindowResize = function(w, h) end
